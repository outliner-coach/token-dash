# PROGRESS

작업 후 이 파일을 갱신한다. 최신이 위.

## 현재 상태 (2026-09-11 기준)

**정상.** 빌드 환경 복구(아래) + 한도 게이지에 "경과 시간" 바깥 링 추가.

### 2026-09-11 (2) — 한도 게이지에 경과 시간 링 추가

- 요청: 5시간·주간 게이지에서 사용률(%)뿐 아니라 "윈도우가 얼마나 지났는지"도 보여서
  진도율(페이스)을 눈으로 비교하고 싶다는 요청
- [OverviewView.swift](Sources/OverviewView.swift) 의 `GaugeSpec.ring()` 에 바깥쪽 얇은 링
  추가: 안쪽 굵은 링(기존) = 한도 사용률, 바깥 얇은 링(신규) = 이번 윈도우 경과 시간 비율
  (5시간 블록은 리셋까지 남은 시간 기준, 주간은 주간 윈도우 시작 대비 경과 기준)
- 바깥 링 색은 페이스 신호로 사용: 사용률이 경과 시간 비율보다 3%p 이상 앞서면
  주황(과속 경고), 아니면 회색(정상 페이스) — `GaugeSpec.paceColor`
- "리셋까지" 행에 "(시간 N% 경과)" 를 병기해 숫자로도 확인 가능. 카드 상단에 두 링의 의미를
  설명하는 캡션 한 줄 추가
- 데이터 없는(비활성) 게이지에서는 바깥 링을 그리지 않음(muted 가드)
- 검증: `./build.sh` 재빌드 성공, 앱 재실행 확인. computer-use 화면 캡처는 사용자가 거절해
  육안 스크린샷 검증은 못했음 — 기하 계산(반지름·여백)으로 겹침/잘림 없는지 코드 리뷰로 확인

### 2026-09-11 — CLT 업데이트 후 빌드 실패 복구

- 증상: `@State` 에서 `SwiftUIMacros.StateMacro` 플러그인을 못 찾아 컴파일 실패, 앱 실행 불가
  (`build.sh` 가 먼저 `rm -rf` 하므로 이전 바이너리도 사라짐)
- 원인: CLT(Swift 6.4) 기본 SDK 심링크가 **MacOSX27.0.sdk(베타)** 를 가리키고, 27 SDK 는
  SwiftUI 프로퍼티 래퍼(`@State` 등)를 **매크로**로 정의함. 매크로 플러그인 dylib 은 Xcode 에만
  있고 CLT 에는 없음(`host/plugins` 에 Observation·Swift 매크로만 존재). 호스트는 macOS 26.6.2
- 조치: `build.sh` 가 호스트 macOS 메이저 버전과 같은 SDK(`MacOSX26.sdk`)가 있으면 `SDKROOT` 로
  고정. 26 SDK 는 `@State` 가 여전히 struct 라 플러그인 불필요. 빌드·실행 확인
- 주의: 이후 macOS 27 로 올라가면 27 SDK 를 쓰게 되므로 그때 다시 깨질 수 있음 → Xcode 설치
  또는 `-plugin-path` 로 SwiftUIMacros 확보 필요

## 이전 상태 (2026-09-03 기준)

**Codex 탭 추가 완료.** 앱 정상 동작(201MB). Claude 탭들은 변경 없음.

### 2026-09-03 — Codex 사용량 별도 탭 추가

- `~/.codex/sessions` 실측: **69.0억 토큰** (2026-03-14~09-03, 활동 66일, 765파일 / 56,273 이벤트)
  · 모델 gpt-5.6-sol 56.8% / gpt-5.5 9.1% / gpt-5.4 8.0% · 프로젝트 account-assist 28.8억,
  math_assist 22.6억 · 캐시 적중 94.6% (실제 새 입력 3.68억)
- **한도는 로그에 임베드** — `token_count.rate_limits` 에 5시간/7일 공식 사용률+리셋시각이
  있어 Claude 때 고생한 OAuth·429 백오프가 불필요. 게이지 2개로 표시
- 집계 함정 4가지 해결 (누계→증가분, 필드 포함관계, 파일명≠내용 날짜, 파일당 다중 세션)
  — 상세는 CLAUDE.md 의 "Codex 집계" 절
- 검증: 독립 Python 구현과 **모든 토큰 수치 완전 일치**. 일별만 1일 차이났는데 Python 이
  UTC 문자열, Swift 가 KST 귀속이라 Swift 쪽이 맞음(기존 탭과 동일 기준). 스캔 2.5초
- **다계정 대응 추가**: 사용자가 Codex 계정 2개를 동시 사용 → 계정 선택 칩(전체/plus/prolite/
  team/미상) 추가, 선택 시 한도 게이지·KPI·모델·프로젝트·일별이 모두 그 계정 기준으로 바뀜
  - 세션 로그에 계정 식별자가 **없음**(session_meta 키 전수 확인) → `rate_limits.plan_type` 을
    계정 대용으로 사용. 파일 내 last-seen 이어쓰기 + 헤드 소급으로 귀속률 83%(미상 17%)
  - 실측: plus 23.5억(34%, 현 로그인 cirano84@gmail.com) / prolite 19.4억(28%) / team 14.2억(21%)
  - 한도도 계정별로 분리 — team 5시간 99%(2일 전), plus 46%, prolite 0%. 이전 구현은 전역
    최신값 하나만 써서 **남의 계정 한도를 보여줄 수 있었음**(수정됨)
  - 한계: 같은 플랜 계정이 둘 이상이면 합쳐짐. auth.json 은 현재 계정 1개만 알려줌
- 미결: gpt-5.x 요율 미확인으로 비용 미표시 · 모델 미상 11.6%(`turn_context` 누락 구간)

## 이전 상태 (2026-07-27 낮 기준)

**정상.** 공식 % 연결 복구됨 — 실측: 5시간 14% · 주간 52% · Fable 42% (모두 limits[] 공식값).
열린 항목은 7/23 목록에서 변동 없음 (setup-token 등록이 여전히 1순위 권고).

### 2026-07-27 (오후) — setup-token 등록 사고와 자가치유 파서

- 사용자가 setup-token 을 Keychain 에 등록했으나 **토큰 중간에 줄바꿈이 낀 채 저장**됨
  (터미널 줄바꿈 복사 사고). security 는 비인쇄 바이트가 섞이면 값 전체를 hex 로 출력하므로
  앱의 형식 검사(`sk-ant-` 접두)가 실패해 조용히 건너뜀 → 세션 토큰 폴백 → 429 겹침
- 수리 중 실수 기록: 검증 전에 정제값을 원본 위에 덮어써서 원본(완전하지만 오염된 값)을
  잃음 — 잘린 79자만 남아 재발급 필요해짐. **교훈: 파괴적 쓰기 전에 반드시 검증**
- 대책 3중화: ① 앱 파서 자가치유(hex 복원 + 토큰 문자만 이어붙임 + 길이 ≥90 검증)
  ② 등록 명령을 `pbpaste | tr -cd 'A-Za-z0-9_-'` 로 교체(클립보드 자동 정제, README 반영)
  ③ 폴링 10분 → **30분** 완화(주말 내내 10분 주기로 429 반복 실측 — 자체 유발 가능성),
  ⌘R 은 즉시 시도 유지
- 사용자 재발급·재등록 대기 중. 그 전에도 살아있는 세션 토큰 체인으로 자동 재연결됨

### 2026-07-27 — 미연결 장애 수리: 토큰 후보 체인

- **증상**: 공식 사용률 "미연결" 지속 (authentication_error)
- **원인**: 나흘간 데스크톱 세션이 쌓여 서로 다른 토큰을 가진 claude 프로세스 5종 공존.
  기존 코드는 `pgrep | head -20` 첫 매치 = **가장 오래된(7/24 금) 프로세스의 죽은 토큰**만 계속 시도
- **발견**: env 토큰은 세션 단위로 보임(scope `user:sessions:claude_code`) — 창을 닫으면
  프로세스가 남아도 토큰이 무효화됨. 실제로 오늘 최신 프로세스 토큰 2종이 401, 일요일부터
  살아있는 세션의 토큰이 유효했음 → "최신 = 유효" 가정도 틀림
- **수정**: 프로세스 젊은 순 최대 4종 + 전용 Keychain + 직전 성공 캐시의 **후보 체인**.
  401/403 → 다음 후보, 429/네트워크 → 즉시 중단(레이트리밋 절약). 전부 만료 시 안내 문구에
  재시작/setup-token 권고 표시
- 헤드리스 검증: 후보 4종 중 3번째로 연결 성공 확인 후 배포

## 이전 상태 (2026-07-23 밤 기준)

**완성·검증됨.** 앱 실행 중, 게이지 3종 모두 공식 % 연결 확인:

| 게이지 | 공식 | Claude 설정 대조 |
|---|---:|---|
| 5시간 | 81% (severity: warning) | 80% — 분당 ~0.5%p 상승 중이라 스냅샷 시차, 일치 판정 |
| 주간 · 전체 | 12% | 12% ✓ |
| 주간 · Fable | 15% | 15% ✓ (limits[] weekly_scoped 버킷) |

로컬 파싱은 독립 Python 구현과 0.003% 이내 일치, 공식 % 파서는 캡처한 실응답으로 단위검증(미분류 0건).

## 열린 항목

1. **(선택) `claude setup-token` 등록** — 지금은 실행 중 Claude 세션의 env 토큰에 의존.
   세션을 다 끄면 공식 % 미연결로 폴백(보정 추정 표시). 사용자가 원하면:
   `claude setup-token` → `security add-generic-password -a "$USER" -s ClaudeUsageDashboard-token -w "<토큰>" -U`
2. **429 공유 관찰** — usage 엔드포인트 레이트리밋이 김. 집 기기의 Claude Tuner 클라이언트와
   계정 한도 공유 추정. 백오프로 공존 중이나, 만성화되면 Tuner 폴링 주기 조정 권고.
3. **azit Tuner 수집기 수리** — 별도 태스크(task_805d08c7)로 발행, 사용자가 별도 세션에서 시작함.
   이 프로젝트 범위 아님.
4. **Fable 계량 방식(학술적 미결)** — 실효 캡이 낮은 것(A)인지 비용 가중(B)인지 확정 못함.
   라이브 증거는 B 우세(아래 7/23 항목). 공식 %를 직접 표시하므로 실용적으론 종결.
5. **집 기기 로그 병합(대기)** — `extraRoots` 기능 구현·검증 완료. 사용자가 집 컴퓨터
   `~/.claude/projects` 를 복사해 오면 config 에 경로만 추가하면 됨.

## 이력

### 2026-07-23 (밤) — 공식 사용량 API 직결

- Tuner 역공학 결과("공식 %의 기록기")를 바탕으로, Claude Code `/usage` 가 쓰는
  `api.anthropic.com/api/oauth/usage` 를 앱이 직접 폴링하도록 구현 (`Official.swift`)
- 토큰: 실행 중 claude 프로세스 env 에서 추출(Keychain 항목은 33일 전 만료 상태였음)
- 실응답 스키마 확보: **`limits[]` 가 정본, Fable 전용 % 존재**(weekly_scoped). 파서가 배열을
  안 타고 `percent` 키를 몰라 놓쳤던 것 수정. `resets_at` 마이크로초 파싱 수정
- 레이트리밋 실측(10분+): 폴링 10분 + 지수 백오프(최대 60분) + ⌘R 수동 갱신
- **라이브 계량 증거**: 이 세션이 Fable 로 전환된 직후 로컬 출력 토큰당 공식 % 소모가 ~3배
  (19:29 로컬 1.59M↔37% → 21:00 로컬 ~2.2M↔81%) → 비용 가중 계량(B) 우세
- 게이지 UI: 공식 % 우선 + "로컬 추정" 병기(차이 = 타 기기+가중 효과), 주간 초반 외삽 노이즈 억제

### 2026-07-23 (저녁) — Tuner 역공학과 7월 일별 검증

- azit 의 SA 자격증명으로 Tuner API 라이브 조회 (`period=1..25` 차분 → 계정 일별 복원)
- **발견: `est_tokens = Δ공식% × 130,000`** (일별값 25개 중 23개가 130K 배수) — Tuner 는 토큰
  실측이 아니라 공식 % 기록기, Max 20x 주간 = 13M 가정
- 7월 일별 대조(드리프트 반증 테스트): **원격(집/모바일) 사용 확정 11일**(주말·금·평일저녁,
  일 0.3~1.4M). "최근 7일 원격≈0" 이전 결론은 원격(+2.9M)과 스냅샷 지연(-2.9M)의 우연 상쇄로 판명
- 주간 한도 재역산 W≈16.2M (87% 앵커 + 원격 보정). Tuner 응답에서 opus/sonnet 필드 소멸 발견
  → azit 수집기 0 적재 문제, 수리 태스크 발행
- `extraRoots` (다기기 로그 병합) 구현, 사본 병합으로 이중계상 0건 검증

### 2026-07-23 (오후) — 한도 단위 교정: raw → 출력 토큰

- 사용자 제공 정본(Claude 설정·Claude Tuner)과 대조: **플랜 한도는 출력 토큰 근사**
  (Tuner 주간 12.3M ↔ 로컬 출력 12.5M, raw 총계는 190배 과대)
- 개요를 두 단위로 분리: 한도(출력) vs 사용 분석(총 처리, 캐시 포함)
- 한도 보정: 주간 12.08M → (원격 반영) 16.2M · 5시간 4.33M(37% 앵커) · Fable 2.46M(100% 시점 실측)
- 오판 정정 기록: raw 기반 "Fable 3배 프리미엄" 주장(폐기) → "프리미엄 없음" 단정(폐기)
  → 최종적으로 A/B 판별불가로 정리(이후 밤에 B 우세 증거 확보)

### 2026-07-22~23 (초기 구축)

- `~/.claude/projects` 스캐너(mmap 바이트 스캔, 2,437파일/2.2GB/3초) + 2026년 집계
- **파싱 3함정 실측·해결**: (message.id+requestId) 중복 55% 제거 / output_tokens 그룹 최대값
  (첫줄 채택 시 18% 누락) / subagents 경로 부모 귀속(가짜 프로젝트 38.8억 토큰 회수)
- 독립 Python 구현과 교차검증 0.003% 일치
- 개요·프로젝트·모델·세션·일별 5탭 UI, 5시간 블록 게이지 → 주간·Fable 게이지 확장
- 보정(calibration) 체계: 사용자 화면의 %로 한도 역산, `~/.config/claude-usage/config.json`
